# Guard Skill

> Combine `careful` and `freeze`.

## Purpose

Use this when a task is both risky and easy to sprawl. `guard` means:

- apply destructive-command caution
- activate or respect a freeze root
- require explicit verification after the change

## Default Use

Use for risky debugging, config work, security-sensitive changes, or any task where unrelated edits would be costly.
